import React from 'react'
import { useLanguage } from '@/src/context/Language'

const OrderDetailPriceRow = () => {
  const { getTranslation } = useLanguage()

  return <div>{/* Rest of the component code */}</div>
}

export default OrderDetailPriceRow
